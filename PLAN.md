# 余生账单实现计划

## 已确认目标

实现一个 React 19 + Vite + TypeScript + Tailwind CSS 的纯前端单页应用。产品是“余生账单”，本地 `resource/hero.mp4` 用作首屏视频；AI 建站提示词只作为纯黑、液态玻璃、编辑式斜体排版与动效的视觉参考。

## 视觉论点

把时间计算器做成一张黑色纪录片式的人生账单：视频制造情绪，大数字制造冲击，玻璃控件只服务于输入和结果。

## 内容结构

1. 全屏 Hero：产品名、问题、预设入口。
2. 配置控制台：基础年龄、工作制度、假期与生活损耗。
3. 结果账单：工作、自由、生存三类时间及多粒度切换。
4. 分享导出：URL 状态同步、9:16 海报预览和下载。

## 交互论点

- Hero 标题逐词由模糊到清晰。
- 页面区块进入视口时轻量显现，结果数字随输入实时变化。
- 液态玻璃按钮和输入提供克制的光晕与位移反馈。

## 验收

- React 19 + pnpm 安装、构建和测试通过。
- Hero 使用本地视频且保留离线可用的 poster。
- 任意配置变化立即重算并同步 URL。
- 预设、调休模型、自定义假期、跨天工时、结果粒度和海报下载可用。
- 桌面与移动端页面无横向溢出，首屏使用 100dvh。

## 匿名留言后台增强（2026-08-28 已确认）

- 服务端通过一年期 `HttpOnly` Cookie 为浏览器分配匿名身份；随机名称只在后台可见，前台不展示且用户不可修改。
- 后台留言接口每页 15 条，支持按状态筛选，并对随机名称和留言正文进行模糊搜索。
- 后台展示随机名称、精确留言时间、完整留言正文与公开状态，并提供上一页、下一页分页。
- 管理员可按匿名用户设置 `1 小时 / 1 天 / 7 天 / 30 天 / 永久 / 自定义截止时间` 禁言，也可立即解除。
- 被禁言浏览器仍可浏览和点赞；提交留言时服务端拒绝并返回剩余禁言时间。
- 公共接口、管理接口与核心策略测试作为本次功能的验收边界；最终应用远程 D1 migration 并部署到 `sun-to-spark.pages.dev`。

## 留言交互微调（2026-08-28 已确认）

- 点赞动效不再使用单一圆环扩散；改为爱心弹性点亮、双细弧掠过、四枚微光上浮、数字轻微上翻和卡片液体短暂提亮。
- 动效总时长控制在约 650ms，不引入粒子库，不干扰瀑布滚动，并遵循 `prefers-reduced-motion`。

## 点赞切换与逻辑删除（2026-08-28 已确认）

- 点赞以匿名浏览器身份为准，再次点击取消点赞并扣减计数；保留旧 IP 点赞记录的兼容迁移路径。
- 后台每条留言显示点赞数，每 10 秒自动同步，支持最新、点赞从高到低和点赞从低到高排序。
- 删除改为 `deleted_at` 逻辑删除；“全部”仅展示未删除留言，“已删除”作为独立入口并支持恢复到删除前状态。
- 已删除留言不在前台时间线出现，也不能继续点赞；不提供永久删除入口。
